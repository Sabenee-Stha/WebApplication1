@{
    ViewBag.Title = "DescribeMySelf";
}

<h2>Describe Myself</h2>

<p>About WebDesign ICT 272</p>

<style>
    /* Style for the overall table */
    table {
        border-collapse: collapse; /* Merges borders together */
        width: 100%; /* Makes the table span the full width of its container */
        font-family: Arial, sans-serif; /* A clean, readable font */
        box-shadow: 0 2px 5px rgba(0,0,0,0.1); /* Adds a subtle shadow for depth */
    }

    /* Style for table headers */
    th {
        background-color: #f2f2f2; /* Light grey background for headers */
        font-weight: bold; /* Makes header text bold */
        text-align: left;
        padding: 12px;
    }

    /* Style for table data cells */
    td {
        padding: 12px;
        border-bottom: 1px solid #ddd; /* Only a bottom border for a clean look */
    }

    /* Style for table rows (for hover effect) */
    tr:hover {
        background-color: #f5f5f5; /* Highlights a row when the mouse is over it */
    }

    /* Removes the bottom border from the last row in the table body */
    tbody tr:last-child td {
        border-bottom: none;
    }
</style>

<table>
    <thead>
        <tr>
            <th>Category</th>
            <th>Details</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td><strong>ID</strong></td>
            <td>20028597</td>
        </tr>
        <tr>
            <td><strong>Class</strong></td>
            <td>207</td>
        </tr>
        <tr>
            <td><strong>Tutor</strong></td>
            <td>Sukbir SINGH</td>
        <tr>
        <tr>
            <td><strong>Full_Name</strong></td>
            <td>Sabin Shrestha</td>
        <tr>

    </tbody>
</table>